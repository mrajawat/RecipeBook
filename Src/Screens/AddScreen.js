import React from 'react';
import {Text, View} from 'react-native';

const AddScreen = () => {
    return (
        <View style={{flex: 1}}>
            <Text>AddScreen</Text>
        </View>
    );
};

export default AddScreen;
